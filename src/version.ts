/**
 * The released version of denomine-mcp.
 *
 * release-please rewrites it on each release PR, which is why it carries the
 * marker comment and must stay a plain literal on one line; the updater matches
 * a semver-looking string on the annotated line and would not find one built
 * from constants.
 */
export const VERSION = "0.3.0"; // x-release-please-version
